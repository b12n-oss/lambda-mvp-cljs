# lambda-mvp-cljs guide

- [The managed runtime](managed-runtime.md) -- why there's no custom
  Runtime API loop here, the `:node-library` + `:exports` wiring, and
  the `unchecked-get` gotcha.
- [Cold vs. warm boot](cold-warm-boot.md) -- the real three-way
  comparison across all three `lambda-mvp-*` siblings.

See also the [project README](https://github.com/b12n-oss/lambda-mvp-cljs/blob/main/README.md).
